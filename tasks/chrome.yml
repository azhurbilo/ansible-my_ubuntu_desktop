---

- name: add chrome key
  apt_key:
    url: 'https://dl-ssl.google.com/linux/linux_signing_key.pub'


- name: add chrome repo
  apt_repository:
    repo: 'deb http://dl.google.com/linux/chrome/deb/ stable main'

- name: install latest chrome package
  apt:
    name: 'google-chrome-stable'
    update_cache: yes
    state: latest
